<!doctype html>
<html lang="zh-CN">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>注册 / 登录</title>
  <style>
    body { font-family: Arial, sans-serif; padding: 20px; max-width: 480px; margin:auto; }
    input { display:block; width:100%; padding:8px; margin:8px 0; box-sizing:border-box }
    button { padding:8px 12px; margin-top:6px }
    .msg { color: red; margin-top:6px }
    .success { color: green }
    hr { margin: 18px 0 }
    a { color: #0366d6 }
  </style>
</head>
<body>
  <h1>注册 / 登录</h1>

  <section id="register">
    <h2>注册</h2>
    <input id="reg-email" type="email" placeholder="邮箱" />
    <input id="reg-pass" type="password" placeholder="密码（建议至少 6 位）" />
    <button id="btn-register">注册</button>
    <div id="reg-msg" class="msg"></div>
  </section>

  <hr/>

  <section id="login">
    <h2>登录</h2>
    <input id="log-email" type="email" placeholder="邮箱" />
    <input id="log-pass" type="password" placeholder="密码" />
    <button id="btn-login">登录</button>
    <div id="log-msg" class="msg"></div>
  </section>

  <hr/>

  <section id="forgot">
    <h2>忘记密码</h2>
    <input id="forgot-email" type="email" placeholder="请输入注册时的邮箱" />
    <button id="btn-forgot">发送重置邮件</button>
    <div id="forgot-msg" class="msg"></div>
  </section>

  <p style="margin-top:18px">登录后会自动跳转到 <code>/auth/dashboard.html</code>（示例）。</p>

  <script type="module" src="./js/firebase-auth.js"></script>
</body>
</html>
